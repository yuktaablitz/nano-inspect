.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
artifacts/cache/
artifacts/edge.db
artifacts/failure_inputs/
artifacts/models/vlm_lora/*.safetensors
cloud/cloud.db
previous_run_bottle_only/*.pt
# hackathon handout photos contain device credentials: never publish
guide/
# model weights are published separately (GitHub release / Hugging Face), too large for git
artifacts/models/
artifacts/logs/
_dryrun.ipynb
artifacts/certs/
config/sop_history/
# the repository holds code + the automation scripts only; everything below stays local
# (measured results and splits ship as a release asset fetched by setup.sh)
artifacts/
docs/
METRICS.md
previous_run_bottle_only/
nanoinspect_tier2ft.ipynb
mvtec_edge_inspector.ipynb
*.zip
